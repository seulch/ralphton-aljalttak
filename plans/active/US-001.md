# US-001: Project scaffolding and Supabase schema

## Plan
- Step 1: Initialize Next.js 15 with App Router + TypeScript + Tailwind CSS v4 using pnpm
- Step 2: Install all dependencies (openai, @supabase/supabase-js, serpapi, node-fetch)
- Step 3: Create Supabase migration SQL for 5 tables with all constraints/indexes/RLS
- Step 4: Execute migration against Supabase via REST API
- Step 5: Create TypeScript types in src/types/database.ts
- Step 6: Create .env.example with all required env vars
- Step 7: Verify pnpm dev runs and pnpm typecheck passes

## Dependencies
- Requires: pnpm, Node.js, Supabase project (URL + anon key in .env)

## Risks
- Tailwind CSS v4 has new config format — need to use @tailwindcss/postcss
- Supabase migration must be EXECUTED not just written
- OPENAI_API_KEY is empty in .env — won't block scaffolding but will block AI features later
